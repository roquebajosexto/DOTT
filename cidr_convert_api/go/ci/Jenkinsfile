pipeline {
	agent any
		stages {
			stage('One') {
				steps {
					sh '''
						echo "Step One"
            pwd
            ls
            cd ./cidr_convert_api/go/
            sudo docker-compose up -d --build
					'''
				}
			}
		}
}